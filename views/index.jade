extends base/layout

block header

block content
	img
	section#sec-home
		span#peerId(data-id=id, hidden)
		div.side
			input(type="text", placeholder="nombre amigo")
			ul
				each friend, i in onlineFriends
					li #{friend}
			a.link-gray Añadir amigo
		div.conversation
			video#video-local(autoplay)
			video#video-remote(autoplay)

block js
	script
		init()
